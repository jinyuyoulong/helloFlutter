main(List<String> args) {
  // 1. 列表 数组 list
  // 2. 集合 set
  // 3. 映射 对象 字典 map

  var names = ["1", "2", "3"];
  print(names[1]);
  var movies = {"星际穿越", "xiyou", "道明空间"};
  print(movies);
  var info = {"name": "fan", "age": 18}; // key 必须可hash
  print(info);
}
